{
  "name": "tsc-test",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.4.0-dev.20210731"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream"
  }
}
